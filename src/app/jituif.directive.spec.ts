import { JituifDirective } from './jituif.directive';

describe('JituifDirective', () => {
  it('should create an instance', () => {
    const directive = new JituifDirective();
    expect(directive).toBeTruthy();
  });
});
